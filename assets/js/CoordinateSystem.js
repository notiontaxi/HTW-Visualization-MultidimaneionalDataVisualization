/*
GUI class
*/

window.CoordinateSystem = CoordinateSystem; 
"use strict";

  function CoordinateSystem(canvas){
    this.canvas = canvas;
  }


  CoordinateSystem.prototype.meashured = function(xVals, yVals){
    // use:
    xVal.min
    xVal.max
    yVal.min
    yVal.max
    canvas.drawLine()
  }




